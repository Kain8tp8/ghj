export type CFMenu = {
    id: number,
    name: string
    photo: string
    orderName: string
    price: string
}
  
export type User = {
    id: number 
    name: string
    surname: string
    bothdey: string
    email: string
    possword: string
    photo: string
  }